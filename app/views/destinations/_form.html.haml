= bootstrap_form_for(@destination, label_errors: true) do |form|
  = form.text_field :campus, placeholder: "Campus", hide_label: true
  = form.text_field :building, placeholder: "Building", hide_label: true

  = form.submit
